{% extends 'base.html.twig' %}

{% block title %}Lecon{% endblock %}

{% block body %}
    <h1>Lecon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMaitreArme</th>
                <td>{{ lecon.idMaitreArme }}</td>
            </tr>
            <tr>
                <th>IdTireur</th>
                <td>{{ lecon.idTireur }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ lecon.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lecon_index') }}">back to list</a>

    <a href="{{ path('lecon_edit', {'id': lecon.id}) }}">edit</a>

    {{ include('lecon/_delete_form.html.twig') }}
{% endblock %}
